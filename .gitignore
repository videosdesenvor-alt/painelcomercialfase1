# dependencies
node_modules

# build output
dist
dist-ssr
*.local
*.tsbuildinfo

# env (a anon key é pública, mas mantemos fora do git; ver .env.example)
.env
.env.*
!.env.example

# vercel
.vercel

# claude / tooling
.claude

# os / editor
.DS_Store
*.log

# Estratégia interna — não versionar em repo público
checklist para escalar.md
